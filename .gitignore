# --- DocMancer local index (generated, not source) ---
# Used locally to fetch & index the pi docs. The indexed SQLite DB and
# extracted markdown/json are throwaway artifacts — re-generated by running
# `docmancer index <source>` in the user's environment.
.docmancer/

# --- Node ---
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# --- TypeScript build artifacts (in case a build step is added later) ---
dist/
build/
*.tsbuildinfo

# --- OS / editor cruft ---
.DS_Store
Thumbs.db
*.swp
*.swo
*~
.vscode/
.idea/

# --- Env / local secrets (if any are added later) ---
.env
.env.local
.env.*.local
